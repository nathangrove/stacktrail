# Example environment variables for running StackTrail with docker-compose
# Copy this file to `.env` and fill in secrets before running `docker compose up`.

# Public host (used by Traefik routing)
STACKTRAIL_HOST=stacktrail.example.com
LETSENCRYPT_EMAIL=ops@example.com

# Database (MySQL)
MYSQL_PASSWORD=change-me

# Session configuration (server cookie signing)
# Generate a new secret (examples):
#  - openssl rand -base64 32
#  - python -c 'import secrets; print(secrets.token_urlsafe(32))'
# Note: keep the secret private and do NOT commit real secrets into git
# SESSION_SECRET=
# SESSION_SECURE=true
# SESSION_SAME_SITE=lax # or 'none' for cross-site, requires SESSION_SECURE=true and HTTPS
# SESSION_MAX_AGE_MS=86400000 # session duration in milliseconds (default: 1 day)
NODE_ENV=development
PORT=4000

# Database selection: "sqlite" or "mysql" (defaults to sqlite)
DB_TYPE=sqlite

# SQLite config (used when DB_TYPE=sqlite)
SQLITE_DB_PATH=data.db

# MySQL connection (used when DB_TYPE=mysql)
#MYSQL_HOST=127.0.0.1
#MYSQL_PORT=3306
#MYSQL_USER=stacktrail
#MYSQL_PASSWORD=stacktrail
#MYSQL_DATABASE=stacktrail

# Dev only: proxy the Vite dev server through the API server so you can use
# http://localhost:4000 for both API and UI while keeping Vite HMR.
#PROXY_WEB_DEV=1
#WEB_DEV_ORIGIN=http://localhost:5173